Show the health status of the EditorLens MK-12 system:

1. Check if dev servers are running on ports 3000 (dashboard), 8000 (backend), 4200 (animation engine) using `lsof -i :PORT`
2. Check if Neo4j is running on port 7687
3. Show git status (branch, uncommitted changes) for the monorepo
4. Show a summary table of each service's status
